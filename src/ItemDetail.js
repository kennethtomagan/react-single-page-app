import React from 'react';
import './App.css';

function Items() {
    useEffect(() => {

    }, []);

    const [item, setItem] = useState({});

    const fetchItem = async () => { };
    return (
        <div>
            <h1>Items Page</h1>
        </div>
    );
}

export default Items;
